// OfflineBanner is now superseded by SyncStatusBar which handles all sync/offline states.
// This component is kept as a simple alias to avoid breaking existing imports.
export { default } from "@/components/SyncStatusBar";